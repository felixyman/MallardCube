"use strict";
/*
 * Copyright Contributors to the Malloy project
 * SPDX-License-Identifier: MIT
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.QOpDescView = void 0;
const malloy_types_1 = require("../../../model/malloy_types");
const static_space_1 = require("../field-space/static-space");
const qop_desc_1 = require("../query-properties/qop-desc");
const query_property_interface_1 = require("../types/query-property-interface");
const view_1 = require("./view");
/**
 * A view operation consisting of literal view operations.
 *
 * e.g. `{ limit: 10 }`
 */
class QOpDescView extends view_1.View {
    constructor(operation) {
        super({ operation });
        this.operation = operation;
        this.elementType = 'qopdesc-view';
    }
    pipelineComp(fs, isNestIn) {
        const newOperation = this.operation.getOp(fs, isNestIn);
        return {
            pipeline: [newOperation.segment],
            outputStruct: newOperation.outputSpace.structDef(),
        };
    }
    getOp(inputFS, isNestIn, qOpDesc, refineThis) {
        if ((0, malloy_types_1.isRawSegment)(refineThis)) {
            this.logError('refinement-of-raw-query', 'A raw query cannot be refined');
            return refineThis;
        }
        qOpDesc.refineFrom(refineThis);
        return qOpDesc.getOp(inputFS, isNestIn).segment;
    }
    refine(inputFS, _pipeline, isNestIn) {
        const pipeline = [..._pipeline];
        if (pipeline.length === 0) {
            return pipeline;
        }
        if (pipeline.length === 1) {
            this.operation.refineFrom(pipeline[0]);
            return [this.getOp(inputFS, isNestIn, this.operation, pipeline[0])];
        }
        const headRefinements = new qop_desc_1.QOpDesc([]);
        const tailRefinements = new qop_desc_1.QOpDesc([]);
        for (const qop of this.operation.list) {
            switch (qop.queryRefinementStage) {
                case query_property_interface_1.LegalRefinementStage.Head:
                    headRefinements.push(qop);
                    break;
                case query_property_interface_1.LegalRefinementStage.Single:
                    qop.logError('illegal-multistage-refinement-operation', 'Illegal in refinement of a query with more than one stage');
                    break;
                case query_property_interface_1.LegalRefinementStage.Tail:
                    tailRefinements.push(qop);
                    break;
                default:
                    qop.logError('illegal-refinement-operation', 'Illegal query refinement');
            }
        }
        if (headRefinements.notEmpty()) {
            this.has({ headRefinements });
            pipeline[0] = this.getOp(inputFS, undefined, headRefinements, pipeline[0]);
        }
        if (tailRefinements.notEmpty()) {
            const last = pipeline.length - 1;
            this.has({ tailRefinements });
            // The changes above should not be allowed to change the input struct
            // for the final segment meaningfully, so we can continue to use the existing
            // second-to-last struct
            const finalIn = pipeline.length > 1
                ? new static_space_1.StaticSourceSpace(pipeline[pipeline.length - 2].outputStruct, 'public')
                : inputFS;
            pipeline[pipeline.length - 1] = this.getOp(finalIn, undefined, tailRefinements, pipeline[last]);
        }
        return pipeline;
    }
    getImplicitName() {
        return undefined;
    }
}
exports.QOpDescView = QOpDescView;
//# sourceMappingURL=qop-desc-view.js.map
"use strict";
/*
 * Copyright Contributors to the Malloy project
 * SPDX-License-Identifier: MIT
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.ViewRefine = void 0;
const error_factory_1 = require("../error-factory");
const refine_utils_1 = require("./refine-utils");
const view_1 = require("./view");
/**
 * A view operation that represents the refinement of one view
 * with another view.
 *
 * e.g. `by_carrier + { limit: 10 }`
 */
class ViewRefine extends view_1.View {
    constructor(base, refinement) {
        super({ base, refinement });
        this.base = base;
        this.refinement = refinement;
        this.elementType = 'refine';
    }
    pipelineComp(fs, isNestIn) {
        const query = this.base.pipelineComp(fs);
        const resultPipe = this.refinement.refine(fs, query.pipeline, isNestIn);
        return {
            pipeline: resultPipe,
            annotations: query.annotations,
            outputStruct: resultPipe.length > 0
                ? resultPipe[resultPipe.length - 1].outputStruct
                : error_factory_1.ErrorFactory.structDef,
        };
    }
    refine(inputFS, pipeline, isNestIn) {
        const refineFrom = this.pipelineComp(inputFS, isNestIn);
        if (refineFrom.pipeline.length !== 1) {
            this.refinement.logError('refinement-with-multistage-view', 'refinement must have exactly one stage');
            // TODO better error pipeline?
            return pipeline;
        }
        return (0, refine_utils_1.refine)(this, pipeline, refineFrom.pipeline[0]);
    }
    getImplicitName() {
        return this.base.getImplicitName();
    }
}
exports.ViewRefine = ViewRefine;
//# sourceMappingURL=view-refine.js.map
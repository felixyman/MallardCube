"use strict";
/*
 * Copyright Contributors to the Malloy project
 * SPDX-License-Identifier: MIT
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.Drill = exports.DrillField = void 0;
exports.updateNestedDrillPaths = updateNestedDrillPaths;
exports.attachDrillPaths = attachDrillPaths;
const composite_source_utils_1 = require("../../composite-source-utils");
const malloy_types_1 = require("../../../model/malloy_types");
const utils_1 = require("../../utils");
const expr_compare_1 = require("../expressions/expr-compare");
const permissive_space_1 = require("../field-space/permissive-space");
const view_field_1 = require("../field-space/view-field");
const expression_def_1 = require("../types/expression-def");
const field_space_1 = require("../types/field-space");
const query_property_interface_1 = require("../types/query-property-interface");
const space_field_1 = require("../types/space-field");
const filters_1 = require("./filters");
class DrillField extends expression_def_1.ExpressionDef {
    constructor(expr, typeDesc) {
        super();
        this.expr = expr;
        this.typeDesc = typeDesc;
        this.elementType = 'drillField';
    }
    getExpression(_fs) {
        return {
            value: this.expr,
            ...this.typeDesc,
        };
    }
}
exports.DrillField = DrillField;
class Drill extends filters_1.Filter {
    constructor() {
        super(...arguments);
        this.elementType = 'drill';
        this.forceQueryClass = undefined;
        this.queryRefinementStage = query_property_interface_1.LegalRefinementStage.Head;
    }
    checkedDrillCondition(fs, filter, reference, value) {
        var _a, _b;
        const permissiveFS = new permissive_space_1.PermissiveSpace(fs);
        const normalLookup = fs.lookup(reference.list);
        if (normalLookup.found) {
            const cond = super.checkedFilterCondition(fs, filter);
            if (cond)
                return cond;
            return undefined;
        }
        if (!fs.isQueryFieldSpace()) {
            // This is really an internal error, this should not happen
            filter.logError('illegal-drill', 'Drill only allowed in query');
            return;
        }
        const drillDimensions = fs.outputSpace().drillDimensions;
        let collectedWheres = undefined;
        let collectedWhereFieldUsage = undefined;
        if (reference.list.length === 0) {
            filter.logError('invalid-drill-reference', 'Invalid drill reference`');
            return;
        }
        else if (reference.list.length < 2) {
            filter.logError('invalid-drill-reference', 'Drill reference be a view name followed by a path to a field in that view, e.g. `some_view.some_nest.some_field`');
            return;
        }
        const [viewName, ...path] = reference.list;
        const viewLookup = fs.lookup([viewName]);
        // TODO register reference to view
        if (viewLookup.found === undefined) {
            viewName.logError('drill-view-reference-not-found', `No such view \`${viewName.refString}\``);
            return;
        }
        if (viewLookup.found.refType !== 'field') {
            viewName.logError('drill-view-reference-not-field', `Head of drill reference must be a view, not a ${viewLookup.found.refType}`);
            return;
        }
        const typeDesc = viewLookup.found.typeDesc();
        if (typeDesc.type !== 'turtle' ||
            !(viewLookup.found instanceof view_field_1.ViewField)) {
            const typeName = (0, malloy_types_1.isAtomic)(typeDesc) ? typeDesc.type : 'join'; // TODO test this?
            filter.logError('drill-view-reference-not-view', `Head of drill reference must be a view, not a ${typeName}`);
            return;
        }
        const fieldName = path[path.length - 1];
        let currentView = viewLookup.found.fieldDef();
        let previousName = viewName;
        let fieldDef = undefined;
        let compareField = undefined;
        const requiredDimensions = [];
        const pathSoFar = [viewName.name];
        for (const name of path) {
            if (currentView.pipeline.length > 1) {
                previousName.logError('drill-incompatible-segment', '`drill:` may not be used with multi-segment views');
                return;
            }
            // TODO can you even `drill` with a pipelined view?
            const segment = currentView.pipeline[currentView.pipeline.length - 1];
            if (segment.type === 'index') {
                previousName.logError('drill-incompatible-segment', 'Index segments are not compatible with `drill:`');
                return;
            }
            else if (segment.type === 'partial') {
                previousName.logError('drill-incompatible-segment', 'Partial segments are not compatible with `drill:`');
                return;
            }
            else if (segment.type === 'raw') {
                previousName.logError('drill-incompatible-segment', '`drill:` is not compatible with SQL queries');
                return;
            }
            const segmentDimensions = segment.queryFields
                .map(f => {
                let fieldDef = undefined;
                if (f.type === 'fieldref') {
                    const morePathFieldNames = f.path.map(n => new field_space_1.FieldName(n));
                    this.has({ morePathFieldNames });
                    const lookup = permissiveFS.lookup(morePathFieldNames);
                    if (lookup.found && lookup.found instanceof space_field_1.SpaceField) {
                        fieldDef = lookup.found.fieldDef();
                        if (fieldDef &&
                            (0, malloy_types_1.isAtomic)(fieldDef) &&
                            (0, malloy_types_1.expressionIsScalar)(fieldDef.expressionType))
                            return f.path[f.path.length - 1];
                    }
                    return undefined;
                }
                else {
                    if ((0, malloy_types_1.isAtomic)(f) && (0, malloy_types_1.expressionIsScalar)(f.expressionType))
                        return (0, malloy_types_1.activeName)(f);
                }
            })
                .filter(utils_1.isNotUndefined);
            requiredDimensions.push(...segmentDimensions.map(f => [...pathSoFar, f]));
            // Only collect filters from nests if they haven't been collected in a previous drill clause
            if (!drillDimensions.some(n => (0, composite_source_utils_1.pathBegins)(n.nestPath, pathSoFar))) {
                const wheres = (_a = segment.filterList) !== null && _a !== void 0 ? _a : [];
                for (const filter of wheres) {
                    if ((0, malloy_types_1.expressionIsAggregate)(filter.expressionType))
                        continue;
                    if (collectedWheres === undefined) {
                        collectedWheres = filter.e;
                        collectedWhereFieldUsage = (0, malloy_types_1.fieldUsageFrom)(filter.refSummary);
                    }
                    else {
                        collectedWheres = {
                            node: 'and',
                            kids: {
                                left: collectedWheres,
                                right: filter.e,
                            },
                        };
                        collectedWhereFieldUsage = (0, composite_source_utils_1.mergeFieldUsage)(collectedWhereFieldUsage, (0, malloy_types_1.fieldUsageFrom)(filter.refSummary));
                    }
                }
            }
            const field = segment.queryFields.find(f => {
                if (f.type === 'fieldref') {
                    return f.path[f.path.length - 1] === name.refString;
                }
                else {
                    return (0, malloy_types_1.activeName)(f) === name.refString;
                }
            });
            if (field === undefined) {
                if (name === fieldName) {
                    name.logError('drill-field-not-found', `No such field \`${name.refString}\` found in \`${previousName.refString}\``);
                }
                else {
                    name.logError('drill-nest-not-found', `No such nest \`${name.refString}\` found in \`${previousName.refString}\``);
                }
                return;
            }
            if (name === fieldName) {
                if (field.type === 'fieldref') {
                    const pathFieldNames = field.path.map(n => new field_space_1.FieldName(n));
                    this.has({ pathFieldNames });
                    const nestLookup = permissiveFS.lookup(pathFieldNames);
                    if (nestLookup.found === undefined) {
                        name.logError('drill-view-reference-not-found', `No such field \`${name.refString}\``);
                        return;
                    }
                    if (!(nestLookup.found instanceof space_field_1.SpaceField)) {
                        name.logError('drill-field-reference-not-field', 'Final element of drill path must be a field');
                        return;
                    }
                    const theFieldDef = nestLookup.found.fieldDef();
                    if (theFieldDef === undefined || (0, malloy_types_1.isAtomic)(theFieldDef)) {
                        fieldDef = theFieldDef;
                        compareField = { node: 'field', path: field.path };
                    }
                    else {
                        name.logError('drill-field-reference-not-field', 'Final element of drill path must be a field');
                        return;
                    }
                }
                else if ((0, malloy_types_1.isAtomic)(field)) {
                    fieldDef = field;
                    compareField = field.e;
                }
            }
            else {
                let nestDef = undefined;
                if (field.type === 'fieldref') {
                    const pathFieldNames = field.path.map(n => new field_space_1.FieldName(n));
                    this.has({ pathFieldNames });
                    const nestLookup = permissiveFS.lookup(pathFieldNames);
                    if (nestLookup.found === undefined) {
                        name.logError('drill-view-reference-not-found', `No such view ${name.refString}`);
                        return;
                    }
                    if (!(nestLookup.found instanceof view_field_1.ViewField)) {
                        name.logError('drill-view-reference-not-view', 'Middle elements of drill path must be nested views');
                        return;
                    }
                    nestDef = nestLookup.found.fieldDef();
                }
                else if (field.type === 'turtle') {
                    nestDef = field;
                }
                else {
                    name.logError('drill-view-reference-not-view', 'Middle elements of drill path must be nested views');
                    return;
                }
                currentView = nestDef;
                previousName = name;
                pathSoFar.push(name.name);
            }
        }
        if (fieldDef === undefined || compareField === undefined) {
            filter.logError('invalid-drill-reference', 'Could not determine drill field');
            return;
        }
        const isAggregate = (0, malloy_types_1.expressionIsAggregate)(fieldDef.expressionType);
        const isAnalytic = (0, malloy_types_1.expressionIsAnalytic)(fieldDef.expressionType);
        if (isAnalytic) {
            filter.logError('analytic-in-drill', 'Analytic expressions are not allowed in `drill:`');
            return;
        }
        else if (isAggregate) {
            filter.logError('aggregate-in-drill', 'Aggregate expressions are not allowed in `drill:`');
        }
        // Add entries for all the other dimensions (to make sure they get satisfied eventually)
        for (const dimensionPath of requiredDimensions) {
            let drillDimension = drillDimensions.find(drillDimension => {
                return (0, composite_source_utils_1.pathEq)(drillDimension.dimensionPath, dimensionPath);
            });
            if (drillDimension === undefined) {
                drillDimension = {
                    nestPath: dimensionPath.slice(0, -1),
                    firstDrill: filter,
                    dimensionPath,
                    satisfied: false,
                };
                drillDimensions.push(drillDimension);
            }
        }
        // Find the entry for this one, and mark it as satisfied
        const dimensionPath = reference.list.map(f => f.name);
        const drillDimension = drillDimensions.find(drillDimension => {
            return (0, composite_source_utils_1.pathEq)(drillDimension.dimensionPath, dimensionPath);
        });
        if (drillDimension !== undefined) {
            drillDimension.satisfied = true;
        }
        const drillField = new DrillField(compareField, {
            ...fieldDef,
            evalSpace: 'output',
            expressionType: (_b = fieldDef.expressionType) !== null && _b !== void 0 ? _b : 'scalar',
            refSummary: fieldDef.refSummary,
        });
        filter.has({ drillField });
        const fExpression = new expr_compare_1.ExprCompare(drillField, '=', value);
        this.has({ fExpression });
        const fExpr = fExpression.getExpression(permissiveFS);
        // TODO valid type of filter condition to be only boolean
        const exprCond = {
            node: 'filterCondition',
            code: filter.exprSrc,
            isSourceFilter: false,
            e: collectedWheres === undefined
                ? fExpr.value
                : {
                    node: 'and',
                    kids: { left: collectedWheres, right: fExpr.value },
                },
            expressionType: fExpr.expressionType,
            refSummary: (0, composite_source_utils_1.mergeRefSummaries)(fExpr.refSummary, (0, malloy_types_1.mkRefSummary)({ fieldUsage: collectedWhereFieldUsage })),
            stableFilter: {
                kind: 'literal_equality',
                expression: {
                    kind: 'field_reference',
                    name: fieldName.refString,
                    path: reference.list.slice(0, -1).map(f => f.name),
                },
                value: value.getStableLiteral(),
            },
        };
        return exprCond;
    }
    checkedFilterCondition(fs, filter) {
        const drillFilter = filter.drillFilter();
        if (drillFilter !== undefined) {
            return this.checkedDrillCondition(fs, filter, drillFilter.reference, drillFilter.value);
        }
        else {
            return super.checkedFilterCondition(fs, filter);
        }
    }
}
exports.Drill = Drill;
function updateNestedDrillPaths(nest, name) {
    if (nest.pipeline.length !== 1 || !(0, malloy_types_1.isQuerySegment)(nest.pipeline[0])) {
        return nest;
    }
    return {
        ...nest,
        pipeline: [
            {
                ...nest.pipeline[0],
                queryFields: nest.pipeline[0].queryFields.map(f => {
                    var _a;
                    if (f.type === 'turtle') {
                        return updateNestedDrillPaths(f, name);
                    }
                    const existing = f.drillExpression;
                    if (existing === undefined)
                        return f;
                    if (existing.kind !== 'field_reference') {
                        return { ...f, drillExpression: undefined };
                    }
                    return {
                        ...f,
                        drillExpression: {
                            ...existing,
                            path: [name, ...((_a = existing.path) !== null && _a !== void 0 ? _a : [])],
                        },
                    };
                }),
            },
        ],
    };
}
function attachDrillPaths(pipeline, nestName) {
    var _a;
    if (pipeline.length !== 1)
        return pipeline;
    if (!(0, malloy_types_1.isQuerySegment)(pipeline[0]))
        return pipeline;
    return [
        {
            ...pipeline[0],
            filterList: (_a = pipeline[0].filterList) === null || _a === void 0 ? void 0 : _a.map(f => ({
                ...f,
                filterView: nestName,
            })),
            queryFields: pipeline[0].queryFields.map(f => {
                if (f.type === 'turtle') {
                    return updateNestedDrillPaths(f, nestName);
                }
                const fieldName = f.type === 'fieldref' ? f.path[f.path.length - 1] : (0, malloy_types_1.activeName)(f);
                return {
                    ...f,
                    drillExpression: {
                        kind: 'field_reference',
                        name: fieldName,
                        path: [nestName],
                    },
                };
            }),
        },
    ];
}
//# sourceMappingURL=drill.js.map
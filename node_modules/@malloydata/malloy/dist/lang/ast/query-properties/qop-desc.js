"use strict";
/*
 * Copyright Contributors to the Malloy project
 * SPDX-License-Identifier: MIT
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.QOpDesc = void 0;
const index_builder_1 = require("../query-builders/index-builder");
const project_builder_1 = require("../query-builders/project-builder");
const reduce_builder_1 = require("../query-builders/reduce-builder");
const malloy_element_1 = require("../types/malloy-element");
const query_property_interface_1 = require("../types/query-property-interface");
const partial_builder_1 = require("../query-builders/partial-builder");
const utils_1 = require("../../utils");
class QOpDesc extends malloy_element_1.ListOf {
    constructor() {
        super(...arguments);
        this.elementType = 'queryOperation';
    }
    computeType() {
        var _a;
        let guessType;
        let needsExplicitQueryClass = false;
        if (this.refineThis) {
            if (this.refineThis.type === 'reduce') {
                guessType = query_property_interface_1.QueryClass.Grouping;
            }
            else if (this.refineThis.type === 'project') {
                guessType = query_property_interface_1.QueryClass.Project;
            }
            else if (this.refineThis.type === 'index') {
                guessType = query_property_interface_1.QueryClass.Index;
            }
        }
        for (const el of this.list) {
            if (el.forceQueryClass) {
                if (guessType) {
                    if (guessType !== el.forceQueryClass) {
                        el.logError(`illegal-${guessType}-operation`, `Use of ${(0, utils_1.modernizeTermsForUserText)(el.forceQueryClass)} is not allowed in a ${(0, utils_1.modernizeTermsForUserText)(guessType)} query`);
                    }
                }
                else {
                    guessType = el.forceQueryClass;
                }
            }
            needsExplicitQueryClass || (needsExplicitQueryClass = (_a = el.needsExplicitQueryClass) !== null && _a !== void 0 ? _a : false);
        }
        if (guessType === undefined && needsExplicitQueryClass) {
            this.logError('ambiguous-view-type', {});
            guessType = query_property_interface_1.QueryClass.Project;
        }
        this.opClass = guessType;
        return guessType;
    }
    refineFrom(existing) {
        this.refineThis = existing;
    }
    getBuilder(baseFS, isNestIn, astEl) {
        switch (this.computeType()) {
            case query_property_interface_1.QueryClass.Grouping:
                return new reduce_builder_1.ReduceBuilder(baseFS, this.refineThis, isNestIn, astEl);
            case query_property_interface_1.QueryClass.Project:
                return new project_builder_1.ProjectBuilder(baseFS, this.refineThis, isNestIn, astEl);
            case query_property_interface_1.QueryClass.Index:
                return new index_builder_1.IndexBuilder(baseFS, this.refineThis, isNestIn, astEl);
            case undefined:
                return new partial_builder_1.PartialBuilder(baseFS, this.refineThis, isNestIn, astEl);
        }
    }
    getOp(inputFS, isNestIn) {
        const build = this.getBuilder(inputFS, isNestIn, this);
        for (const qp of this.list) {
            build.execute(qp);
        }
        const segment = build.finalize(this.refineThis);
        return {
            segment,
            outputSpace: build.resultFS,
        };
    }
}
exports.QOpDesc = QOpDesc;
//# sourceMappingURL=qop-desc.js.map
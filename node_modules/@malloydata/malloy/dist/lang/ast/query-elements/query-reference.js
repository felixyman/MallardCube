"use strict";
/*
 * Copyright Contributors to the Malloy project
 * SPDX-License-Identifier: MIT
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.QueryReference = void 0;
const error_factory_1 = require("../error-factory");
const malloy_element_1 = require("../types/malloy-element");
const query_head_struct_1 = require("./query-head-struct");
const malloy_types_1 = require("../../../model/malloy_types");
/**
 * A query operation that is just a reference to an existing query.
 *
 * e.g. after the colon in `run: flights_by_carrier`
 */
class QueryReference extends malloy_element_1.MalloyElement {
    constructor(name) {
        super();
        this.name = name;
        this.elementType = 'query-reference';
    }
    queryComp(isRefOk) {
        const headEntry = this.modelEntry(this.name);
        const query = headEntry === null || headEntry === void 0 ? void 0 : headEntry.entry;
        const oops = function () {
            return {
                inputStruct: error_factory_1.ErrorFactory.structDef,
                outputStruct: error_factory_1.ErrorFactory.structDef,
                query: error_factory_1.ErrorFactory.query,
            };
        };
        if (!query) {
            this.logError('query-reference-not-found', `Reference to undefined query '${this.name.refString}'`);
            return oops();
        }
        if (query.type === 'query') {
            const queryHead = new query_head_struct_1.QueryHeadStruct(query.structRef, query.sourceArguments);
            this.has({ queryHead: queryHead });
            const inputStruct = queryHead.getSourceDef(undefined);
            const outputStruct = query.pipeline[query.pipeline.length - 1].outputStruct;
            const unRefedQuery = isRefOk
                ? query
                : (0, malloy_types_1.refIsStructDef)(query.structRef)
                    ? query
                    : { ...query, structRef: inputStruct };
            return {
                query: unRefedQuery,
                outputStruct,
                inputStruct,
            };
        }
        this.logError('non-query-used-as-query', `Illegal reference to '${this.name}', query expected`);
        return oops();
    }
    query(isRefOk = true) {
        return this.queryComp(isRefOk).query;
    }
}
exports.QueryReference = QueryReference;
//# sourceMappingURL=query-reference.js.map
"use strict";
/*
 * Copyright Contributors to the Malloy project
 * SPDX-License-Identifier: MIT
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.SourceQueryElement = void 0;
const malloy_element_1 = require("../types/malloy-element");
/**
 * An AST element which can be treated as either a source or a query
 * depending on context. For instance, an `SQReference` represents
 * a model-level reference to an entity which is either a source or
 * a query.
 */
class SourceQueryElement extends malloy_element_1.MalloyElement {
    constructor() {
        super(...arguments);
        this.errored = false;
    }
    getSource() {
        return;
    }
    getQuery() {
        return;
    }
    isSource() {
        return false;
    }
    sqLog(code, parameters, options) {
        if (this.isErrorFree()) {
            this.logError(code, parameters, options);
        }
        this.errored = true;
        return code;
    }
    isErrorFree() {
        if (this.errored) {
            return false;
        }
        let clean = true;
        for (const child of this.walk()) {
            if (child instanceof SourceQueryElement && child.errored) {
                clean = false;
                break;
            }
        }
        return clean;
    }
}
exports.SourceQueryElement = SourceQueryElement;
//# sourceMappingURL=source-query-element.js.map
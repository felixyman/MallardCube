"use strict";
/*
 * Copyright Contributors to the Malloy project
 * SPDX-License-Identifier: MIT
 */
Object.defineProperty(exports, "__esModule", { value: true });
exports.SQExtend = void 0;
const refined_source_1 = require("../source-elements/refined-source");
const source_query_element_1 = require("./source-query-element");
/**
 * An element which represents adding source extensions to a
 * query or source using the `extend` operator. This element
 * cannot be treated as a query.
 *
 * e.g. `flights extend { rename: carrier2 is carrier }`
 */
class SQExtend extends source_query_element_1.SourceQueryElement {
    constructor(sqSrc, extend, includeList) {
        super({ sqSrc, extend });
        this.sqSrc = sqSrc;
        this.extend = extend;
        this.includeList = includeList;
        this.elementType = 'sq-extend';
    }
    getSource() {
        if (this.asSource) {
            return this.asSource;
        }
        const src = this.sqSrc.getSource();
        if (src) {
            this.asSource = new refined_source_1.RefinedSource(src, this.extend, this.includeList);
            this.has({ asSource: this.asSource });
            return this.asSource;
        }
        this.sqLog('failed-to-compute-source-to-extend', 'Could not compute source to extend');
    }
    isSource() {
        return true;
    }
}
exports.SQExtend = SQExtend;
//# sourceMappingURL=sq-extend.js.map
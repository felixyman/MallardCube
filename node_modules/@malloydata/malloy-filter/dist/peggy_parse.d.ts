import type { FilterExpression, FilterLog } from './filter_interface';
export declare function run_parser(src: string, parse: (input: string) => unknown): {
    parsed: FilterExpression | null;
    log: FilterLog[];
};

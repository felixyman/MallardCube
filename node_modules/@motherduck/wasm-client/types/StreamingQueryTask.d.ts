import { AsyncDuckDBConnection } from './duckdb-wasm';
import { Task } from './motherduck/task-sequencer';
import { StreamingQueryResult } from './QueryTypes';
export declare class StreamingQueryTask implements Task<StreamingQueryResult> {
    private connection;
    private deferredConnection;
    private query;
    private args?;
    constructor(connection: Promise<AsyncDuckDBConnection>, query: string, args?: any[]);
    run(): Promise<StreamingQueryResult>;
    cancel(): Promise<boolean>;
    private getConnection;
    private sendQuery;
}

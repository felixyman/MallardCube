import { DuckDBDecimalFormatOptions } from './conversion/stringFromDecimal.js';
import { SpecialDuckDBValue } from './SpecialDuckDBValue.js';
export declare class DuckDBDecimal extends SpecialDuckDBValue {
    readonly scaledValue: bigint;
    readonly scale: number;
    constructor(scaledValue: bigint, scale: number);
    toDuckDBString(): string;
    /** Returns a string representation appropriate to the host environment's current locale. */
    toLocaleString(locales?: string | string[], options?: DuckDBDecimalFormatOptions): string;
    toJS(): string;
}

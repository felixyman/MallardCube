import { DuckDBData } from '../../motherduck/duckdb-data';
import { DuckDBType } from '../../motherduck/duckdb-types';
import { DuckDBValue } from '../../motherduck/duckdb-values';
import { RecordBatch } from 'apache-arrow';
import { TypedTypeMap } from './TypedTypeMap.js';
export declare class DuckDBRecordBatch extends DuckDBData {
    private recordBatch;
    constructor(recordBatch: RecordBatch<TypedTypeMap>);
    get columnCount(): number;
    get rowCount(): number;
    columnName(columnIndex: number): string;
    columnType(columnIndex: number): DuckDBType;
    value(columnIndex: number, rowIndex: number): DuckDBValue;
}

/target
CONTEXT.md
debug-last-run.log
xmla-trace.jsonl
mallard-crash.log
mallard-last-requests.log
*.db
*.duckdb
node_modules/
build/
conversion-inventory.json
conversion-inventory.md
skills-lock.json
.slim/deepwork/

# Local agent tooling (installed per-developer, not part of the project).
# The repo's own review agent is the exception: it travels with the project so
# every session gets the same reviewer (plan 051).
.opencode/*
!.opencode/agents/
!.opencode/agents/**
opencode.json.backup
.agents/skills/excel-mcp/

# Private/local models: never commit (plan 044 — generic examples only).
# Only the sample projects below belong to the repo; anything else dropped
# under projects/ stays local.
projects/*
!projects/generated_contoso/
!projects/generated_retail_analytics/
!projects/project2/
!projects/project3/
!projects/project4/
!projects/upstream_marts/
